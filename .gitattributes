# LF in the repository for every text file, on every platform.
* text=auto eol=lf

# Binary assets are never normalized.
*.png binary
*.jpg binary
*.gif binary
*.webp binary
*.woff binary
*.woff2 binary
